---
- name: Write HCLOUD_TOKEN to app .env
  lineinfile:
    path: "{{ app_dir }}/.env"
    regexp: "^HCLOUD_TOKEN="
    line: "HCLOUD_TOKEN={{ hcloud_token }}"
    create: true
    mode: "0600"
  when: hcloud_token is defined and hcloud_token | length > 0
  no_log: true
