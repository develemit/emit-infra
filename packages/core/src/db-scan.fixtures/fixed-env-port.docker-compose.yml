services:
  postgres-e2e:
    image: postgres:16-alpine
    container_name: eta-postgres-e2e
    environment:
      POSTGRES_USER: eta
      POSTGRES_PASSWORD: eta
      POSTGRES_DB: eta
    ports:
      - "${E2E_PG_PORT:-5436}:5432"
