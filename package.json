{
  "name": "emit-infra",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nx run-many -t dev",
    "launch": "launchctl kickstart -k gui/$(id -u)/com.emit.infra || launchctl bootstrap gui/$(id -u) $HOME/Library/LaunchAgents/com.emit.infra.plist",
    "launch:stop": "launchctl bootout gui/$(id -u)/com.emit.infra",
    "build": "nx run-many -t build",
    "lint": "nx run-many -t lint",
    "typecheck": "nx run-many -t typecheck",
    "test": "nx run-many -t test",
    "test:hooks": "bash scripts/lib/deploy-plan.test.sh && bash scripts/lib/deploy-path-filter.test.sh && bash scripts/lib/docker-build.test.sh && bash scripts/lib/db-url.test.sh && bash scripts/lib/hook-signals.test.sh && bash scripts/lib/deploy-liveness.test.sh && bash scripts/lib/deploy-unattended-gate.test.sh && bash scripts/lib/deploy-detached.test.sh && bash scripts/lib/serve-supervised.test.sh && bash scripts/lib/resource-gate.test.sh && bash scripts/lib/check-all.test.sh && bash scripts/lib/dev-stack-watchdog.test.sh && bash scripts/lib/rotate-launchd-logs.test.sh",
    "graph": "nx graph",
    "tailscale:serve": "tailscale serve --bg --https=7003 http://127.0.0.1:7013",
    "tailscale:stop": "tailscale serve --https=7003 off"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "sharp"
    ]
  },
  "devDependencies": {
    "@next/eslint-plugin-next": "^15.0.0",
    "@nx/eslint": "^21.0.0",
    "@nx/js": "^21.0.0",
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "esbuild": "^0.28.0",
    "eslint": "^9.0.0",
    "nx": "^21.0.0",
    "prettier": "^3.0.0",
    "tsx": "^4.22.4",
    "typescript": "^5.7.0",
    "vitest": "^3.2.6"
  }
}
