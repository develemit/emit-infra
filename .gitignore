# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
tmp/
.nx/cache
.nx/workspace-data
# Accidental literal "~" dir (tool expanded ~ without a shell)
/~/
typecheck-output.txt

# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.lock.info
.terraform.lock.hcl
terraform.tfvars
*.auto.tfvars

# Ansible
*.retry
ansible/inventory/*.ini
ansible/inventory/dynamic/

# Environment
.env
.env.*
!.env.example

# Project configs (contain real domains/names — keep templates, not instances)
.emit-infra.json

# scripts/serve-supervised.sh death records (sprint 310) — runtime state,
# same treatment as .deploy-history.jsonl
.server-deaths.jsonl

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo


design-*